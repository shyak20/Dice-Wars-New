/// <summary>Copy and button labels for <see cref="PrecisionPanel"/> prompts.</summary>
public enum PrecisionPromptPresentation
{
    /// <summary>Classic precision (+X Power, Yes / No).</summary>
    Default = 0,

    /// <summary><see cref="Effects.GameActions.AddPowerAction"/> — ability opt-in (+X Power, Use ability / Ignore).</summary>
    AddPowerAbility = 1,
}
