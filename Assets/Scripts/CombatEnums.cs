using UnityEngine;

// DO NOT PUT A CLASS HERE. These must be global.
public enum DieType { Attack, Defense }
public enum FaceEffect { None, Poison, Heal, ShieldBreak }
//public enum CombatState { WaitingForRoll, Rolling, Resolution, BustCheck, TurnEnd }